/// Generic selectable option used by recipe categories, allergens and setup data.
class AddRecipeOption {
  final String id;
  final String name;

  const AddRecipeOption({
    required this.id,
    required this.name,
  });
}
